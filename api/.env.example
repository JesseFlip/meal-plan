# Copy to .env (or set in your hosting provider) — never commit the real .env

# Backend (api/)
DATABASE_URL=sqlite:///fridgeplan.db
# In production: the Neon Postgres connection string (Render has no persistent disk, so SQLite won't survive a redeploy)

ALLOWED_ORIGINS=http://localhost:5173
# In production: https://your-site.netlify.app
